version: "3.9"
services:
  redis:
    image: bitnami/redis:latest
    container_name: redis
    environment:
      - ALLOW_EMPTY_PASSWORD=yes
    ports:
      - "${REDIS_PORT}:6379"
    volumes:
      - $HOME/BSS/tools/redis-data:/bitnami/redis/data
    restart: always
    networks:
      - bss_dev
networks:
  bss_dev:
    driver: bridge